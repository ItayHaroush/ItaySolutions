/**
 * Buildix (Laravel) — external proposal approvals + quote sync
 *
 * Production:
 *   https://app.buildix.site/api/proposals/approve
 */
window.PROPOSAL_SUBMIT_URL = "https://app.buildix.site/api/proposals/approve";

/** Optional — must match BUILDIX_EXTERNAL_PROPOSAL_API_KEY on the server */
window.PROPOSAL_SUBMIT_API_KEY = "";
